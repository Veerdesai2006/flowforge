
# FlowForge

FlowForge is a FastAPI application scaffold.

## Run locally

```powershell
uv run uvicorn app.main:app --reload
```

The health endpoint is available at `http://127.0.0.1:8000/health`.
